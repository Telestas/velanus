name: CI

# main deja de desplegar al empujar (eso lo hace una etiqueta v*), pero sigue
# verificándose: así un fallo se ve al integrar, no al publicar.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run lint
      - run: npm run build
